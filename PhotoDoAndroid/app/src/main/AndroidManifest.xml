<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yqy.photodo">

    <!-- 访问网路的权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 读取相册权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".FacePhotoHandle.DetectPhotoActivity" />
        <activity android:name=".UserLoginActivity" />
        <activity android:name=".UserRegistActivity" />
        <activity android:name=".DocumentHandle.DetectOrcCardActivity" />
        <activity android:name=".FacePhotoHandle.FaceFeatureActivity" />
        <activity android:name=".FacePhotoHandle.CompareFaceActivity" />
        <activity android:name=".DocumentHandle.BankCardActivity" />
        <activity android:name=".FaceCenter.FaceStorgeActivity"></activity>
    </application>

</manifest>